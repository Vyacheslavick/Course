<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.course">

    <application
        android:name=".SupportClasses.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.SupplyViewActivity"></activity>
        <activity android:name=".Activities.AddToBucketActivity" />
        <activity
            android:name=".Activities.SettingsDetailActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".Activities.AddDetailActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".Activities.AddProviderActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".Activities.HistoryViewActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".Activities.MainActivity"
            android:label="@string/title_activity_main"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>